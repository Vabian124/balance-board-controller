<UserControl x:Class="BalanceBoard.App.Controls.SessionLogPanel" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"><Border x:Name="LiveLogPanel" Style="{StaticResource Card}" Margin="0,8,0,0" Padding="12,8,12,8">
            <Expander x:Name="SessionLogExpander" Background="Transparent" BorderThickness="0">
                <Expander.Header>
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="SESSION LOG" Style="{DynamicResource Text.Section}" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                        <TextBlock x:Name="LogPreviewText" DockPanel.Dock="Left" Margin="12,0,0,0"
                                   Style="{DynamicResource Text.Mono}" FontSize="11" Opacity="0.75"
                                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                   Visibility="Collapsed"/>
                    </DockPanel>
                </Expander.Header>
                <Grid Margin="0,4,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="140"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,6">
                        <Button x:Name="ClearLogViewButton" Content="Clear view" Style="{StaticResource Button.Ghost}" DockPanel.Dock="Right"/>
                        <Button x:Name="OpenLogFolderButton" Content="Open logs folder" Style="{StaticResource Button.Ghost}" DockPanel.Dock="Right" Margin="0,0,8,0"/>
                    </DockPanel>
                    <TextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" AcceptsReturn="True"
                             FontFamily="{StaticResource Font.Mono}" FontSize="12"
                             Background="{DynamicResource Brush.Card}"
                             Foreground="{DynamicResource Brush.Foreground}"
                             BorderBrush="{DynamicResource Brush.CardBorder}" BorderThickness="1"/>
                </Grid>
            </Expander>
        </Border></UserControl>