<Window x:Class="BalanceBoard.App.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Setup Wizard"
        Height="560" Width="760"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Quick Setup" Style="{StaticResource HeroTitle}" FontSize="24"/>
            <TextBlock x:Name="StepTitle" Margin="0,8,0,0" Foreground="{StaticResource TextMutedBrush}" FontSize="15"/>
            <ProgressBar x:Name="StepProgress" Height="6" Margin="0,16,0,0" Maximum="2" Value="0"
                         Background="{StaticResource SurfaceAltBrush}" Foreground="{StaticResource AccentBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,20,0,20" Style="{StaticResource Card}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="StepContent"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BackButton" Content="Back" Style="{StaticResource GhostButton}" Click="BackButton_Click"/>
            <Button x:Name="NextButton" Content="Next" Click="NextButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
