# Update 008 — UI redesign (system themes, cancellable connect)

| Field | Value |
|-------|-------|
| Commit | `5256356` |
| Date | 2026-07-07 |
| Branch | `main` |

## What was done

- Rebuilt WPF UI from scratch: `Themes/Colors.xaml`, `Themes/Controls.xaml`, single-page `MainWindow`
- Removed legacy `ModernTheme.xaml` and `SetupWizardWindow`
- Instant UI on startup; deferred cleanup and Bluetooth warmup
- Cancellable connect via `CancellationToken` + Cancel button
- Dev scripts: `start.ps1`, `stop.ps1`, `restart.ps1`, `connect.ps1`
- CLI flags: `--dev`, `--connect`, `--no-cleanup`, `--allow-multiple`

## Not done / left for later

- Formal edge-case test plan and smart reconnect policy (see update 009)
- Second-instance window activation
