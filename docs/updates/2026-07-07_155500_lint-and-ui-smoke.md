# Update 013 — Lint tooling and UI smoke test

| Field | Value |
|-------|-------|
| Commit | *(set after commit)* |
| Date | 2026-07-07 |
| Branch | `main` |

## What was done

- **Why CornerRadius wasn't caught:** `dotnet build` does not validate `StaticResource` type compatibility; old smoke tests only checked process start, not XAML load
- **`tools/UiSmoke`** — constructs `MainWindow` on STA thread with theme resources; **would catch** runtime XAML errors like `Double` → `CornerRadius`
- **`scripts/lint.ps1`** — format, build `-warnaserror`, Validate, UiSmoke, test-flow
- **`WpfAnalyzers`** NuGet on `BalanceBoard.App` (WPF0041 suppressed — style-only)
- **RapidXaml.BuildAnalysis** evaluated and **removed** (broken MSBuild task on .NET 8 SDK)
- CI updated: format check + UI smoke step

## Not done / left for later

- Compile-time XAML type checking (no stable tool for WPF SDK projects today)
- Optional Rapid XAML VS extension for local dev only
