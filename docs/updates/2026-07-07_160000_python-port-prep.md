# Update 014 — Refactor core for Python port readiness

| Field | Value |
|-------|-------|
| Commit | *(set after commit)* |
| Date | 2026-07-07 |
| Branch | `main` |

## What was done

- **`Processing/ActionEngine.cs`** — portable action-slot state machine
- **`Abstractions/IInputBackend`**, **`IActionSimulator`**
- **`Models/VirtualKeyCodes.cs`**, **`ActionConstants.cs`**
- **`Services/Win32InputBackend.cs`** — Win32 SendInput extracted from InputSimulator
- **WinForms removed** from Core (`UseWindowsForms=false`)
- **Session** accepts optional `IGameControllerOutput` / `IActionSimulator` injection
- **18 golden tests** (jump, diagonal, deadzone, ActionEngine, VirtualKeyCodes)
- **`docs/REFACTORING_FOR_PYTHON.md`** — layer map and port order
- CI + `lint.ps1` run unit tests

## Not done / left for later

- Split `InputSimulator` Win32 backend from portable action engine
- Actual Python implementation (separate project)
