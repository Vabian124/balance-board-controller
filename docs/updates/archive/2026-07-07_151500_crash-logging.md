# Update 015 — Crash logging and poll-path hardening

| Field | Value |
|-------|-------|
| Commit | [`ff60cb2`](https://github.com/Vabian124/balance-board-controller/commit/ff60cb2) |
| Date | 2026-07-07 |
| Branch | `main` |

## What was done

- Reviewed `%AppData%\BalanceBoardApp\logs\session-2026-07-07.log` — last run paired board at 15:07:05 then stopped with no crash entry
- **`FileLogService.WriteException`** — stack traces to `[ERROR]` lines
- **`GlobalExceptionLogging`** — AppDomain, unobserved task, and UI thread fatals logged to file
- **Poll/connect/vJoy/input** — try/catch with stack logging; poll reentrancy guard
- **`BalanceBoardVisual`** — stop allocating `SolidColorBrush` every 50 ms tick
- Unit test for `WriteException`

## Log finding

Pairing succeeded (`Paired 1 Nintendo device(s).`) but no `Connected.` line — likely native/unlogged crash during HID connect or first poll. Future crashes should appear as `[ERROR]` / `FATAL` in the session log.
