# Update 017 — Repo cleanup and reference folder

| Field | Value |
|-------|-------|
| Commit | *(set after commit)* |
| Date | 2026-07-07 |
| Branch | `main` |

## What was done

- Moved `WiiBalanceWalker/` → `reference/WiiBalanceWalker/`
- Removed duplicate tracked DLLs and `vjoy/` / `vjoystick/` copies (canonical DLLs: `libs/x64/`)
- Deleted local junk: `baseline/`, `WiiBalanceWalker.sln`, loose `reference/*.exe` / `*.zip`
- Added `reference/README.md`; updated `.gitignore` and active docs
- Legacy `.csproj` now points at `libs/x64/`

## Not removed

- `docs/updates/` audit log (intentional history)
- `.cursor/rules/` (agent guidance)
- `src/`, `tests/`, `tools/`, `scripts/` — active project layout
